namespace DevDock.Models
{
    // Represents the workflow lanes used by the task board UI.
    public enum TaskStatus
    {
        Backlog,
        Todo,
        InProgress,
        CodeReview,
        Completed
    }
}